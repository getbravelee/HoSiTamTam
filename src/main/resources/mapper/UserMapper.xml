<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.suleekyuri.hositamtam.mapper.UserMapper">

    <!-- 사용자명으로 사용자 조회 -->
    <select id="findByUsername" resultType="com.suleekyuri.hositamtam.user.User">
        SELECT * FROM users WHERE userLoginId = #{username}
    </select>

    <!-- 로그인 ID로 사용자 조회 -->
    <select id="findByLoginId" resultType="com.suleekyuri.hositamtam.user.User">
        SELECT * FROM users WHERE userLoginId = #{userLoginId}
    </select>

    <!-- 사용자 등록 -->
    <insert id="insertUser" parameterType="com.suleekyuri.hositamtam.user.User">
        INSERT INTO users (userLoginId, userLoginPassword, userNickname, userEmail)
        VALUES (#{userLoginId}, #{userLoginPassword}, #{userNickname}, #{userEmail})
    </insert>

</mapper>